"""Neuron on-device agent — reads the firmware bundle, opens parent
channels, enforces safety interlocks autonomously when disconnected.

See ../README.md for the full contract.
"""
__version__ = "0.1.0"
